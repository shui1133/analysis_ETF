# 路徑設定與爬蟲使用指南

## 📁 資料路徑自動設定

系統會**自動偵測作業系統**並使用對應的資料目錄：

### Windows
```
C:\Python\退休理財規劃分析\data
```

### Linux / Mac / GitHub Codespaces
```
/data
```

### 如何修改路徑？

如果您想使用自訂路徑，有兩種方法：

#### 方法 1：修改程式碼（永久）

編輯 `data_fetcher.py` 中的 `get_data_dir()` 函數：

```python
def get_data_dir():
    """
    根據作業系統自動選擇資料目錄
    """
    if platform.system() == 'Windows':
        data_dir = r"D:\我的資料夾\ETF數據"  # 改成您想要的路徑
    else:
        data_dir = "/home/myuser/etf_data"   # 改成您想要的路徑
    
    os.makedirs(data_dir, exist_ok=True)
    return data_dir
```

同時也要修改 `backtest.py` 中相同的函數。

#### 方法 2：執行時指定（臨時）

在程式碼中直接指定：

```python
from data_fetcher import ETFDataFetcher
from backtest import PortfolioBacktest

# 指定自訂路徑
custom_dir = "C:\\我的路徑\\data"

fetcher = ETFDataFetcher(output_dir=custom_dir)
backtester = PortfolioBacktest(data_dir=custom_dir)
```

---

## 🕷️ 多重資料來源爬蟲

系統實作了**四個資料來源**，會按順序嘗試：

### 1. yfinance（優先推薦）⭐

**優點：**
- ✅ 最穩定可靠
- ✅ 完整歷史資料
- ✅ 包含股價和配息
- ✅ 免費無限制

**缺點：**
- ⚠️ 某些新 ETF 資料可能不完整
- ⚠️ 台灣股票格式特殊（.TW 或 .TWO）

**使用範例：**
```python
from data_fetcher import ETFDataFetcher

fetcher = ETFDataFetcher()
data = fetcher._fetch_from_yfinance("0056")

if data:
    print(f"股價資料: {len(data['price_data'])} 筆")
    print(f"配息資料: {len(data['dividend_data'])} 筆")
```

---

### 2. MoneyDJ（次要選擇）

**優點：**
- ✅ 配息資料較準確
- ✅ 台灣專業財經網站
- ✅ 資料更新及時

**缺點：**
- ⚠️ 可能有反爬蟲機制
- ⚠️ 歷史股價資料較難取得
- ⚠️ 需要模擬瀏覽器行為

**使用範例：**
```python
from data_fetcher import ETFDataFetcher

fetcher = ETFDataFetcher()
data = fetcher._fetch_from_moneydj("00878")

if data:
    print(f"資料來源: {data['source']}")
    if data.get('dividend_data') is not None:
        print(f"配息資料: {len(data['dividend_data'])} 筆")
```

**技術細節：**
- 爬取網址：`https://www.moneydj.com/etf/x/basic/`
- 解析配息表格
- 使用 BeautifulSoup 解析 HTML

---

### 3. Goodinfo（補充資料）

**優點：**
- ✅ 配息資料詳細
- ✅ 歷史資料完整
- ✅ 免費使用

**缺點：**
- ⚠️ **嚴格的反爬蟲機制**
- ⚠️ 需要建立 session
- ⚠️ 請求頻率限制

**使用範例：**
```python
from data_fetcher import ETFDataFetcher
import time

fetcher = ETFDataFetcher()

# 需要先訪問首頁建立 session
time.sleep(2)  # 避免太頻繁

data = fetcher._fetch_from_goodinfo("0056")

if data:
    print(f"資料來源: {data['source']}")
    if data.get('dividend_data') is not None:
        print(f"配息資料: {len(data['dividend_data'])} 筆")
```

**技術細節：**
- 爬取網址：`https://goodinfo.tw/tw/StockDividendPolicy.asp`
- 需要模擬完整瀏覽器行為
- 建議請求間隔 2-3 秒

**注意事項：**
⚠️ Goodinfo 有嚴格的反爬蟲機制，包括：
- IP 限制
- User-Agent 檢查
- Session 驗證
- 請求頻率限制

建議：
1. 不要頻繁請求（間隔 2 秒以上）
2. 使用完整的瀏覽器 headers
3. 如果被封鎖，等待一段時間再試

---

### 4. 證交所/櫃買中心（最後備用）

**優點：**
- ✅ 官方資料來源
- ✅ 權威可信

**缺點：**
- ⚠️ 目前為**簡化實作**
- ⚠️ 使用模擬資料
- ⚠️ 待完整實作

**狀態：**
目前使用模擬資料作為最後備用方案。

---

## 🔄 自動容錯機制

系統會**自動**按以下順序嘗試：

```
1. yfinance
   ↓ 失敗或資料不足
2. MoneyDJ
   ↓ 失敗或資料不足
3. Goodinfo
   ↓ 失敗或資料不足
4. 證交所（模擬資料）
```

**範例：**
```python
# 系統會自動嘗試所有來源
data = fetcher.fetch_data("00929")

# 檢查資料來源
print(f"資料來源: {data['source']}")
# 可能的值: 
# - 'yfinance'
# - 'moneydj'
# - 'goodinfo'
# - 'twse+goodinfo' (混合來源)
# - 'simulation' (模擬資料)
```

---

## 🧪 測試爬蟲功能

我們提供了專門的測試腳本：

```bash
python test_crawlers.py
```

這會測試所有資料來源並顯示結果：

```
================================================================================
資料來源測試
================================================================================

================================================================================
測試 0056 (元大高股息)
================================================================================

【測試 yfinance】
✓ 成功
  股價資料: 2456 筆
  配息資料: 48 筆

【測試 MoneyDJ】
⚠️  MoneyDJ 股價資料結構複雜，建議使用 yfinance
✗ 失敗

【測試 Goodinfo】
✓ 成功
  股價資料: 0 筆
  配息資料: 52 筆
```

---

## 📊 資料格式

### 股價資料格式

```python
price_data = pd.DataFrame({
    '日期': [...],      # datetime64
    '開盤價': [...],    # float, 四捨五入至小數點第二位
    '最高價': [...],    # float
    '最低價': [...],    # float
    '收盤價': [...],    # float
    '成交量': [...]     # int
})
```

### 配息資料格式

```python
dividend_data = pd.DataFrame({
    '除息日': [...],    # datetime64
    '股利': [...]       # float, 每股配息金額
})
```

---

## 💾 資料儲存

資料會自動儲存為 CSV 檔案：

### 檔案命名

```
股價：{代碼}_{名稱}.csv
配息：{代碼}_{名稱}_配息.csv
分割：{代碼}_{名稱}_分割.csv
```

### 範例

```
C:\Python\退休理財規劃分析\data\
├── 0056_元大高股息.csv
├── 0056_元大高股息_配息.csv
├── 00878_國泰永續高股息.csv
├── 00878_國泰永續高股息_配息.csv
└── ...
```

---

## ⚙️ 進階設定

### 自訂爬蟲參數

在 `data_fetcher.py` 的 `__init__` 中：

```python
self.headers = {
    'User-Agent': '您的 User-Agent',
    'Accept': 'text/html,application/xhtml+xml',
    # ... 其他 headers
}
```

### 調整請求間隔

在 `fetch_all_etfs` 方法中：

```python
def fetch_all_etfs(self, etf_list):
    results = {}
    for ticker in etf_list:
        data = self.fetch_data(ticker)
        results[ticker] = data
        time.sleep(3)  # 調整為 3 秒（原本 1 秒）
    return results
```

### 新增其他 ETF

在 `etf_info` 字典中新增：

```python
self.etf_info = {
    "0050": {"name": "元大台灣50", "market": "TW"},
    "0051": {"name": "元大中型100", "market": "TW"},
    # ... 新增其他 ETF
}
```

---

## ❗ 重要提醒

### 1. 爬蟲使用道德

- ⚠️ 不要過度頻繁請求
- ⚠️ 遵守網站的 robots.txt
- ⚠️ 避免在尖峰時段大量爬取

### 2. 資料準確性

- ⚠️ 建議驗證重要的配息資料
- ⚠️ yfinance 最穩定但可能有延遲
- ⚠️ 使用官方資料作為最終確認

### 3. 反爬蟲機制

如果遇到以下情況：
- IP 被封鎖
- 請求被拒絕
- 資料無法取得

**解決方案：**
1. 等待一段時間（30 分鐘到數小時）
2. 更換 IP（使用 VPN）
3. 降低請求頻率
4. 使用其他資料來源

---

## 📞 需要協助？

如果遇到問題：

1. **檢查資料目錄**
   ```python
   from data_fetcher import get_data_dir
   print(get_data_dir())
   ```

2. **測試爬蟲**
   ```bash
   python test_crawlers.py
   ```

3. **查看錯誤訊息**
   - 檢查終端機輸出
   - 確認網路連線
   - 驗證 ETF 代碼

4. **參考其他文件**
   - `README.md` - 完整說明
   - `系統說明.md` - 技術細節
   - `QUICKSTART.md` - 快速開始

---

## 🎉 總結

- ✅ 系統會**自動偵測**作業系統並設定路徑
- ✅ Windows: `C:\Python\退休理財規劃分析\data`
- ✅ Linux/Mac: `/data`
- ✅ **四個資料來源**按順序自動嘗試
- ✅ **yfinance** 最穩定推薦
- ✅ **MoneyDJ** 和 **Goodinfo** 作為補充
- ✅ 完整的**容錯機制**

開始使用：
```bash
python app.py
```

祝您使用順利！📈
